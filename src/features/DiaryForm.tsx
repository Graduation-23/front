import {View} from 'react-native';

import {AppText} from '../components/AppText';

type DiaryFormProps = {};

export default function DiaryForm({}: DiaryFormProps) {
  return (
    <View>
      <AppText>{}일기</AppText>
    </View>
  );
}
