import {Text} from '@rneui/themed';
import {View} from 'react-native';
import {SafeAreaView} from 'react-native-safe-area-context';

const UserInfoScreen = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>UserInfoScreen 입니다.</Text>
      </View>
    </SafeAreaView>
  );
};

export default UserInfoScreen;
